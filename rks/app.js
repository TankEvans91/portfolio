const THUMBNAILS = document.querySelector(".thumbnail img");

      THUMBNAILS.forEach((THUMBNAIL) => {
      THUMBNAIL.addEventListener("click", () => console.log (THUMBNAIL));       
            });
